if ball.top <= 0 or ball.bottom >= SCREEN_HEIGHT:
    ball_speed_y *= -1

What is does: checks if the top of bttom of the ball hits the top of bottom of the berrier 
Why: So i am able to simulate when the ball hits the top or the bottom it will bounce the oposite direction
